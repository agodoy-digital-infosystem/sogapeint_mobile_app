name: Test Backend

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Étape 1 : Vérifier le code source
      - name: Checkout repository
        uses: actions/checkout@v3

      # Étape 2 : Configurer Node.js avec la version 20
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # Étape 3 : Installation des dépendances dans le répertoire backend
      - name: Install dependencies
        working-directory: backend
        run: npm ci --include=dev

      # Étape 4 : Exécuter les tests en utilisant le script test du package.json
      - name: Run tests with npm test
        working-directory: backend
        run: npm test
        env:
          CI: true
          
      # Étape 5 : Afficher les logs en cas d'échec
      - name: Show Jest error log
        if: failure()
        run: |
          echo "Erreur lors de l'exécution des tests. Affichage des logs Jest."
          cat backend/tests/logs/jest.log || echo "Log Jest non trouvé."
